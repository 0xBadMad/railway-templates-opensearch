{
  "$schema": "https://railway.app/template-schema.json",
  "name": "OpenSearch",
  "description": "OpenSearch single-node search and analytics engine. Auth enabled by default.",
  "version": "1.0.0",
  "build": {
    "dockerfile": "Dockerfile",
    "nixpacksConfigFile": "nixpacks.toml"
  },
  "deploy": {
    "numReplicas": 1
  },
  "port": 9200,
  "variables": [
    {
      "name": "OPENSEARCH_ADMIN_PASSWORD",
      "description": "Admin password for OpenSearch security. Default: auto-generated secret.",
      "default": "",
      "required": false
    }
  ]
}
